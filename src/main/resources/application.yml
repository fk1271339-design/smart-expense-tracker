server:
  port: 9090

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smart_expense_tracker?useSSL=false&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

app:
  jwt:
    secret: thisIsMySuperSecretJwtKeyChangeItBeforeProduction123456
    expiration-ms: 86400000